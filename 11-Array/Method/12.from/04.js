// from() Method with a Set

// defining a Set
let set = new Set(["JavaScript", "Python", "Go", "Python"]);

// creating an array from the given  set
let result = Array.from(set);

console.log(result);

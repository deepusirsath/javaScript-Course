// The toLocaleString() method returns a string representing the elements of the array in a particular locale.

// Example

let array1 = ["Nepal", 1];

// returns string representation of array
let stringFromArray = array1.toLocaleString();

console.log(stringFromArray);

// Output:
// Nepal,1

//Note:

// This method converts each array element to Strings using their toLocaleString methods
//and separates them by a comma.
